file_client: local

id: k3d

file_roots:
  base: 
    - /srv/salt/base/stack

pillar_roots:
  base:
    - /srv/salt/base/pillar

grains:
  k3d_password: ${k3d_password}
  vplatform: azure

use_superseded:
  - module.run

